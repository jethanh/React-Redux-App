const initialState = {
    message: "",
    status: '',
    isFetching: false,
    error: ''
};

export const dogReducer = (state = initialState, action) => {
    switch(action.type){
        default: 
            return state;
    }
};